import React from 'react';
import PropTypes from 'prop-types';
import styles from ''

const SubmitButton ({ onSubmit }) => {
return(
    <button className={styles.Button} onSubmit >Create 
    Covr Lettr</button>
)
}

export const SubmitButton;
